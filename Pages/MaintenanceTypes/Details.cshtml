@page
@model Proyecto_Laboratorios_Univalle.Pages.MaintenanceTypes.DetailsModel

@{
    ViewData["Title"] = "Detalles del Tipo de Mantenimiento";
}

<h1>Detalles</h1>

<div>
    <h4>Tipo de Mantenimiento</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MaintenanceType.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MaintenanceType.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MaintenanceType.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MaintenanceType.Description)
        </dd>
        <hr />
        <dt class="col-sm-2">
            Auditoría
        </dt>
        <dd class="col-sm-10">
            <small>
                <strong>Creado:</strong> @Model.MaintenanceType.CreatedDate.ToString("dd/MM/yyyy HH:mm")
                @if (Model.MaintenanceType.CreatedBy != null)
                {
                    <span> por @Model.MaintenanceType.CreatedBy.FullName</span>
                }
                <br />
                @if (Model.MaintenanceType.LastModifiedDate.HasValue)
                {
                    <strong>Modificado:</strong> @Model.MaintenanceType.LastModifiedDate.Value.ToString("dd/MM/yyyy HH:mm")
                    @if (Model.MaintenanceType.ModifiedBy != null)
                    {
                        <span> por @Model.MaintenanceType.ModifiedBy.FullName</span>
                    }
                }
            </small>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.MaintenanceType.Id">Editar</a> |
    <a asp-page="./Index">Volver al Listado</a>
</div>
